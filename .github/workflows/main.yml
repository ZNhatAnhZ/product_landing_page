on:
  push:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  run_pull:
    name: run pull
    runs-on: ubuntu-latest
    
    steps: 
    - name: Checkout
      uses: actions/checkout@v3.5.2
      
    - name: connect and pull
      uses: appleboy/ssh-action@v0.1.10
      with: |
        host: 103.179.172.248
        username: root
        password: 2S2BFUqtj2hjNe1W
        script: | 
          cd product_landing_page
          git checkout main
          git pull origin main
        
    - name: run server
      run: python3 -m http.server 8080
    
